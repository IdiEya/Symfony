{% extends 'base.html.twig' %}

{% block title %}Détails du Produit{% endblock %}

{% block body %}
<div class="container py-5" >
    <div class="card shadow-lg">
        <div class="card-header bg-primary text-white">
            <h1 class="h4 mb-0"><i class="fas fa-cube me-2"></i>Fiche produit</h1>
        </div>

        <div class="card-body">
            <div class="row mb-4">
                {% if produit.photo %}
                <div class="col-md-4 text-center">
                    <img src="{{ asset('uploads/photos/' ~ produit.photo) }}" 
                         class="img-fluid rounded mb-3" 
                         alt="Image du produit"
                         style="max-height: 250px;">
                </div>
                {% endif %}
                
                <div class="col-md-{% if produit.photo %}8{% else %}12{% endif %}">
                    <table class="table table-striped table-hover">
                        <tbody>
                           
                            <tr>
                                <th>Nom</th>
                                <td>{{ produit.nom }}</td>
                            </tr>
                            <tr>
                                <th>Référence</th>
                                <td><code>{{ produit.ref }}</code></td>
                            </tr>
                            <tr>
                                <th>Prix</th>
                                <td>{{ produit.prix|number_format(2, ',', ' ') }} DT</td>
                            </tr>
                            <tr>
                                <th>Stock</th>
                                <td>
                                    <span class="badge {{ produit.quantite > 0 ? 'bg-success' : 'bg-danger' }}">
                                        {{ produit.quantite }} unité(s)
                                    </span>
                                </td>
                            </tr>
                            <tr>
                                <th>Catégorie</th>
                                <td>
                                {{ produit.categorie }} 
                                </td>
                            </tr>
                        </tbody>
                    </table>
                </div>
            </div>

            <div class="mb-4">
                <h5 class="mb-3"><i class="fas fa-align-left me-2"></i>Description</h5>
                <div class="border rounded p-3 bg-light">
                    {{ produit.description|default('Aucune description disponible')|nl2br }}
                </div>
            </div>

            <div class="d-flex justify-content-between align-items-center"  >
                <a href="{{ path('app_produits_index') }}" class="btn " >
                    <i class="fas fa-arrow-left me-2"></i>Retour à la liste
                </a>
                
                <div class="d-flex gap-2" style="background-color:rgb(195, 177, 229);" >
                   
                    
                   
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Modal de suppression -->
<div class="modal fade" id="deleteModal" tabindex="-1" aria-labelledby="deleteModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header bg-danger text-white">
                <h5 class="modal-title" id="deleteModalLabel">
                    <i class="fas fa-exclamation-triangle me-2"></i>Confirmation de suppression
                </h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                Êtes-vous sûr de vouloir supprimer définitivement ce produit ?
                <div class="alert alert-warning mt-3">
                    <i class="fas fa-info-circle me-2"></i>
                    Cette action supprimera toutes les données associées de manière irréversible.
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">
                    <i class="fas fa-times me-2"></i>Annuler
                </button>
                {{ include('produits/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
</div>
{% endblock %}